apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-app
  namespace: production
  labels:
    app: finance-app
    tier: backend
    environment: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: finance-app
  template:
    metadata:
      labels:
        app: finance-app
        tier: backend
        environment: production
    spec:
      containers:
      - name: app
        image: localhost:5000/finance-app:production
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: PORT
          value: "8000"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: finance-app
  namespace: production
  labels:
    app: finance-app
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: finance-app
---
apiVersion: v1
kind: Service
metadata:
  name: finance-app-nodeport
  namespace: production
  labels:
    app: finance-app
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30003
    protocol: TCP
    name: http
  selector:
    app: finance-app